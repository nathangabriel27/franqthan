import { Dimensions, StyleSheet, } from 'react-native';
const { height, width } = Dimensions.get('window');
import { colors, sizes, margin, padding } from '../../constants/theme';

export default StyleSheet.create({
  
})